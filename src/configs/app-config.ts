import { ConfigType } from "../types/configs";

const AppConfig: ConfigType = {
  mode: "light",
  sidebarWidth: 250,
  foldedSidebarWidth: 60,
  primaryColor: "#3B76EF",
};

export default AppConfig;
